{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://funora.dev/spec/0.41.0/models/orders-page.schema.json",
  "x-funora-spec-version": "0.41.0",
  "title": "OrdersPage",
  "description": "Результат чтения списка продаж.\n\nЗаписи отдаются МЕТОДОМ, а не полем. Открытый список делает неполноту незаметной: вызывающий берёт его и не спрашивает про полноту, а неполный список неотличим от полного. Метод отказывается отдавать записи неполного результата без явного признания неполноты.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "completeness": {
      "type": "string",
      "enum": [
        "complete",
        "partial",
        "unknown"
      ],
      "x-funora-closed": true,
      "description": "Полнота прочитанного. complete - разобрано всё, что нашлось. partial - часть данных не прочитана. unknown - установить не удалось.\n\nПустой список даёт complete - с 0.7.0, по наблюдению. Страница без продаж снята контрольной парой 24.08.2026: таблицы на ней нет вовсе, вместо неё сообщение в отдельном абзаце, а форма фильтра остаётся. До этого снимка здесь стояло unknown, и цена того незнания была не в полноте: курсор снимается только с полного чтения, и событие о первой продаже поглощалось."
    },
    "reason": {
      "type": "string",
      "x-funora-plain": true,
      "description": "Машиночитаемая причина, по которой полнота такова. Наблюдаемые значения: all_rows_parsed, all_messages_parsed, empty_list_not_observed, empty_thread_not_observed, page_defects, row_defects."
    },
    "observed_at": {
      "x-funora-type": "instant",
      "description": "Момент получения ответа. Точка отсчёта для относительных меток времени на странице: площадка даёт «два часа назад», и без этой точки такая метка не превращается в момент."
    },
    "rows_total": {
      "type": "integer",
      "minimum": 0,
      "description": "Сколько кандидатов в записи нашлось на странице, штук."
    },
    "rows_accepted": {
      "type": "integer",
      "minimum": 0,
      "description": "Сколько записей собрано, штук."
    },
    "rows_rejected": {
      "type": "integer",
      "minimum": 0,
      "description": "Сколько кандидатов отброшено как непригодные, штук."
    },
    "defects": {
      "type": "array",
      "items": {
        "$ref": "https://funora.dev/spec/0.41.0/models/defect.schema.json"
      },
      "description": "Замеченные повреждения. Пустой список означает, что повреждений не нашлось, а не что их не искали."
    },
    "entries": {
      "type": "array",
      "items": {
        "$ref": "https://funora.dev/spec/0.41.0/models/order-list-entry.schema.json"
      },
      "description": "Собранные записи заказов."
    },
    "filters_available": {
      "type": "boolean",
      "description": "Есть ли на странице форма сужения списка.\n\nЧИТАЕТСЯ, А НЕ ПРЕДПОЛАГАЕТСЯ. В снимке 19.08.2026 формы нет вовсе, в снимке 28.08.2026 она есть - площадка показывает её не всегда.\n\nОтправить фильтр туда, где формы нет, значило бы получить несуженный список молча: вызывающий увидел бы чужие заказы там, где просил свои."
    }
  },
  "required": [
    "completeness",
    "reason",
    "observed_at",
    "rows_total",
    "rows_accepted",
    "rows_rejected",
    "defects",
    "entries",
    "filters_available"
  ]
}
