{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://funora.dev/spec/0.16.0/models/session-health.schema.json",
  "x-funora-spec-version": "0.16.0",
  "title": "SessionHealth",
  "description": "Пригодность сессии, установленная самым дешёвым доступным способом.\n\nТИП БЫЛ ОБЪЯВЛЕН ВОЗВРАЩАЕМЫМ И НЕ ИМЕЛ СХЕМЫ до 0.14.0. Реализовать проверку по контракту было нельзя, и сверить ответы двух SDK нечем: имя типа есть, формы нет.\n\nПроверка НЕ выполняет деловой операции ради проверки - лишний запрос это лишний след. Она читает ту же страницу, что и чтение аккаунта, и отвечает по её классу.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "response_class": {
      "type": "string",
      "enum": [
        "ok",
        "login_required",
        "challenge",
        "blocked",
        "rate_limited",
        "maintenance",
        "wrong_identity",
        "transport_error",
        "unknown"
      ],
      "x-funora-closed": true,
      "description": "Класс ответа площадки, каким его определил классификатор.\n\nЗначения различают состояния доступа и поломку разметки: это разные проблемы с разным лечением, а внешне они одинаковы."
    },
    "is_usable": {
      "type": "boolean",
      "description": "Годится ли сессия для работы прямо сейчас.\n\nИстинно РОВНО при классе ok. Прочие классы означают разное, но ни один из них не означает «можно работать»."
    },
    "reason": {
      "type": "string",
      "x-funora-plain": true,
      "description": "Машиночитаемая причина решения классификатора."
    },
    "provisional": {
      "type": "boolean",
      "description": "Принято ли решение НЕПРОВЕРЕННОЙ сигнатурой.\n\nЧасть сигнатур составлена умозрительно: страницы блокировки, проверки и обслуживания никто не видел. Решение по такой сигнатуре отдаётся с пометкой, а не выдаётся за наблюдение."
    },
    "checked_at": {
      "x-funora-type": "instant",
      "description": "Момент проверки."
    },
    "from_cache": {
      "type": "boolean",
      "description": "Отдан ли ответ из кэша, а не получен запросом.\n\nПроверка ставится в цикл ядра, а не зовётся человеком. Без дросселя реализация пошла бы на площадку каждый тик - и это именно то, чего обещание операции запрещает."
    }
  },
  "required": [
    "response_class",
    "is_usable",
    "reason",
    "provisional",
    "checked_at",
    "from_cache"
  ]
}
