{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://funora.dev/schemas/0.43.0/withdrawal-channel.schema.json",
  "x-funora-spec-version": "0.43.0",
  "title": "WithdrawalChannel",
  "description": "Канал вывода: пара «валюта счёта - способ вывода» и сведения о комиссии.\n\nСЛОВА «КОМИССИЯ» В КОНТРАКТЕ ДО 31.08.2026 НЕ БЫЛО НИГДЕ - ни в правилах извлечения, ни в службах, ни в реестрах. Это первое место, где она названа.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "currency",
    "option_key",
    "name",
    "fee_text"
  ],
  "properties": {
    "currency": {
      "type": "string",
      "x-funora-plain": true,
      "description": "Валюта счёта, с которой выводят. Код ISO 4217 прописными."
    },
    "option_key": {
      "type": "string",
      "x-funora-plain": true,
      "description": "Машинное имя способа вывода, к которому ведёт этот канал."
    },
    "name": {
      "$ref": "observed.schema.json",
      "x-funora-observed-value": "text",
      "description": "Название канала на локали интерфейса. Не разбирается."
    },
    "fee_text": {
      "$ref": "observed.schema.json",
      "x-funora-observed-value": "text",
      "description": "Сведения о комиссии, КАК ЕСТЬ.\n\nНЕ РАЗБИРАЮТСЯ. Это текст на локали интерфейса, и выводить из него величину значило бы строить расчёт денег на переводе - при том, что локаль привязана к аккаунту, а не к адресу."
    }
  }
}
