{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://funora.dev/spec/0.6.0/models/account.schema.json",
  "x-funora-spec-version": "0.6.0",
  "title": "Account",
  "description": "Аккаунт продавца.\n\nНИ ОДНА РЕАЛИЗАЦИЯ ЭТОГО НЕ СОБИРАЕТ. Баланс объявлен обязательным, а страница с балансом не наблюдалась.\n\nПометка стоит здесь затем же, зачем у схем непорождаемых событий: схема без реализации описывает намерение, а читается как контракт. Второй SDK, написанный по ней, выдумает то, чего не наблюдал.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "user": {
      "$ref": "https://funora.dev/spec/0.6.0/models/user.schema.json",
      "description": "Профиль владельца сессии."
    },
    "balance": {
      "$ref": "https://funora.dev/spec/0.6.0/models/money.schema.json",
      "x-funora-observability": "unobserved-possible",
      "x-funora-governed-by": "account.balance",
      "description": "Доступный баланс, если показан. Отсутствие значения означает «не наблюдали», а не ноль: возможность account.balance указывает сюда, а это поле - обратно на неё, и снять различие можно только с обеих сторон сразу."
    },
    "csrf_token": {
      "type": "string",
      "x-funora-sensitivity": "secret",
      "description": "Токен формы. Помечен secret: не попадает в repr, логи, телеметрию и диагностику."
    },
    "locale": {
      "type": "string",
      "pattern": "^[A-Za-z]{2,3}(-[A-Za-z0-9]{2,8})*$",
      "description": "Локаль интерфейса, наблюдённая фактически. Перечисление ОТКРЫТО: площадка вправе отдать интерфейс на любом языке, и запись обязана его показать. Прежде набор был закрыт значением supported_locales, а описание обещало отказ при локали вне набора - обещание противоречило правилу извлечения, которое запрещает отказывать из-за локали, и не исполнялось никогда. Поддержанность локали сообщается возможностью protocol.locale, а не отказом."
    },
    "observed_at": {
      "x-funora-type": "instant",
      "description": "Момент получения ответа адаптером. Точка отсчёта для всех относительных меток в этом ответе."
    }
  },
  "required": [
    "user",
    "locale",
    "observed_at"
  ],
  "x-funora-not-implemented": true
}
