{
  "name": "funora-spec",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical contract for the Funora SDKs",
  "license": "Apache-2.0",
  "type": "commonjs",
  "scripts": {
    "validate": "node scripts/validate.js",
    "compat": "node scripts/compat.js",
    "check": "npm run validate && npm run compat",
    "conformance": "node scripts/conformance.js"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
