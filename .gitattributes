# Спецификация сравнивается побайтово: canonical JSON, хэши снимков и ключи
# идемпотентности зависят от содержимого файлов. Перевод строки обязан быть
# одинаковым на всех машинах, иначе Windows-чекаут даст другой хэш.
* text=auto eol=lf

# Двоичное - без нормализации.
*.png binary
*.jpg binary
*.ico binary
